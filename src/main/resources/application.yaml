spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/eshop_db
    username: bestuser
    password: bestuser
  flyway:
    baseline-on-migrate: true